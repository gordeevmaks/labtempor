function calculateResult() {
    // Example calculation that produces a result
    let result = 12345678901234567890; // This could be any large number calculation

    // Convert the result to a BigInt and return it
    return BigInt(result);
}

// Example usage
let bigIntResult = calculateResult();
console.log(bigIntResult); // Output: 12345678901234567890n
